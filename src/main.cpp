#include "tools.hpp"

int main()
{
     normalize_recurrence();
     // int reg[] = {5, 3, 7, 2, 4, 6, 1};
     // Tree *raiz = CreateTree();
     // Tree *aux = CreateTree();
     // Record r;

     // cout << "Arvore vazia: " << TVazia(&raiz) << endl;

     // cout << "ELEMENTOS DA ARVORE: {";

     // for (int i = 0; i < 7; i++)
     // {
     //      r.key = reg[i];
     //      r.value = 1;
     //      insertTree(&raiz, r);
     //      cout << " " << reg[i];
     // }

     // cout << "}" << endl;

     // cout << "Arvore vazia: \n"
     //      << TVazia(&raiz) << endl;

     // cout << "METODO PREORDEM: {";
     // preordem(raiz);
     // cout << "}\n"
     //      << endl;

     // cout << "METODO CENTRAL: {";
     // central(raiz);
     // cout << "}\n"
     //      << endl;

     // cout << "METODO POSORDEM: {";
     // posordem(raiz);
     // cout << "}\n"
     //      << endl;

     // r.key = 2;
     // pesquisa(&raiz, &aux, r);

     // cout << "PESQUISA 2 E IMPRIME A SUBARVORE {";
     // central(aux);
     // cout << "}\n"
     //      << endl;

     // r.key = 3;
     // removeTree(&raiz, r);

     // cout << "REMOVE 3 E IMPRIME A ARVORE { ";
     // central(raiz);
     // cout << "}\n"
     //      << endl;
     return 0;
}